<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Pangolin&display=swap"
      rel="stylesheet"
    />
    <title>Document</title>
    <style>
      * {
        margin: 0;
        padding: 0;
        font-family: "Pangolin";
        transition: 0.3s;
      }
      form {
        width: 100%;
        display: flex;
        flex-direction: column;
        gap: 1rem;
      }
      .form-group {
        display: flex;
        flex-direction: column;
        gap: 0.25rem;
      }
      textarea {
        resize: none;
      }
      .pentru {
        font-size: 2rem;
      }
      #persoana {
        background: none;
        border: none;
        border-bottom: 1px solid #ba274d;
        color: #ba274d;
        outline: none;
        font-size: 2rem;
        white-space: pre;
        padding: 5px;
        transition: 0s;
        width: calc(100% - 1rem);
      }

      #persoana::placeholder {
        font-size: 1.5rem;
      }

      #text {
        outline: none;
        background: none;
        border: none;
        font-size: 1.3rem;
        box-shadow: 1px 1px 20px 0#ba274e57;
        border-radius: 1rem;
        box-sizing: border-box;
        padding: 1rem;
      }
      button {
        border: none;
        background-color: #ba274d;
        color: white;
        box-sizing: border-box;
        padding: 0.5rem 1rem;
        border-radius: 1rem;
      }

      button:hover {
        background-color: white;
        color: #ba274d;
      }

      /* width */
      ::-webkit-scrollbar {
        width: 10px;
      }

      /* Track */
      ::-webkit-scrollbar-track {
        background: none;
      }

      /* Handle */
      ::-webkit-scrollbar-thumb {
        background: #ba274d;
        border-radius: 1rem;
      }

      /* Handle on hover */
      ::-webkit-scrollbar-thumb:hover {
        background: #bd1340;
      }

      .heart {
        width: 50px;
        height: auto;
        position: absolute;
        opacity: 0;
        top: 0;
        left: 0;
      }

      .relative {
        position: relative;
      }

      .relative:hover .heart {
        opacity: 1;
      }

      .heart-1 {
        rotate: -60deg;
      }

      .relative:hover .heart-1 {
        top: -20px;
        left: -80px;
      }

      .heart-2 {
        scale: 0.8;
        rotate: -25deg;
      }

      .relative:hover .heart-2 {
        top: -50px;
        left: -50px;
      }

      .heart-3 {
        scale: 0.8;
        rotate: -90deg;
      }

      .relative:hover .heart-3 {
        top: 25px;
        left: -50px;
      }

      @media only screen and (max-width: 996px) {
        .heart {
          display: none;
        }
      }

      * {
        margin: 0;
        padding: 0;
        font-family: "Pangolin";
      }

      html body {
        background-color: #ffb0f4;
        background-image: url("https://i.ibb.co/gtQh2dC/02-Aesthetic-Valentines-Day-Wallpapers-With-Cute-Hearts-Free-For-PPT-Backgrounds-by-Slides-Corner.webp");
        background-repeat: no-repeat;
        background-position: center;
        background-size: cover;
        backdrop-filter: brightness(90%);
      }

      .content {
        width: 100%;
        max-width: 1200px;
        margin: 0 auto;
        display: flex;
        flex-direction: column;
        gap: 2rem;
        justify-content: center;
        align-items: center;
        padding: 1rem;
        box-sizing: border-box;
      }

      a {
        border: none;
        background-color: #ba274d;
        padding: 1rem;
        box-sizing: border-box;
        border-radius: 1rem;
        color: white;
        text-decoration: none;
      }

      .inceput {
        font-size: 1.2rem;
        height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
      }

      .mb-3 {
        margin-bottom: 1.5rem;
      }

      .fullheight {
        height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
      }
    </style>
  </head>
  <body>
    <div class="content"><%- body -%></div>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.3/jquery.min.js"></script>
  </body>
</html>
